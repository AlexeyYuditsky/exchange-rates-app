package com.alexeyyuditsky.exchange_rates.utils

import com.alexeyyuditsky.exchange_rates.adapters.ConverterAdapter
import com.alexeyyuditsky.exchange_rates.model.currencies.ConverterCurrency

/*
fun calculations(
    code: String,
    value: String,
    currencies: List<ConverterCurrency>,
    adapter: ConverterAdapter
) {
    log("calculations")
    currencies.forEach {
        if (it.code != code) it.valueShow = "999"
        */
/*if (it.code == code) {
            it.valueShow = value
        } else {
            it.valueShow = (value.toFloat() / it.valueToday.toFloat()).toString()
        }*//*

    }

    adapter.notifyItemRangeChanged(1, 153, listOf(null))
}*/
